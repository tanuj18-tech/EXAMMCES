#include<reg51.h>
void delay(unsigned int itime){
	unsigned int i,j;
	for(i=0;i<itime;i++)
	for(j=0;j<1275;j++);
}
 
void main(){
	while(1){
		P2=0x00;
		delay(250);
		P2=0x10;
		delay(250);
		P2=0x20;
		delay(250);
		P2=0x30;
		delay(250);
		}
	}
 